<h1>マイチケット一覧</h1>

<% if @tickets.any? %>
  <ul>
    <% @tickets.each do |ticket| %>
      <li>
        チケットID: <%= ticket.id %>
        /
        状態:
        <%= ticket.used ? "使用済み" : "未使用" %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>チケットはまだありません。</p>
<% end %>
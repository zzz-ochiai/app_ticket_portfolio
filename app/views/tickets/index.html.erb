<h1>チケット一覧</h1>

<% @tickets.each do |ticket| %>
  <p><%= ticket.title %></p>

  <% if ticket.used? %>
    <p>使用済み</p>
  <% else %>
    <p>未使用</p>
    <%= button_to "使用する", ticket_path(ticket), method: :patch %>
  <% end %>
<% end %>